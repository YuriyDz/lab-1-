/*import React from 'react';
import { nanoid } from 'nanoid';
export default class ContactForm extends Component{
  state = {
       
      colect: ,
      
    } 
const Find = (findDeadlines, changeHistor, time) => {      
      findDeadlines.forEach(element => {
        if((Number(element.duration)%Number(time)) === 0){
           const Item = {
             id: nanoid(),
             nameType: element.type,
             timeDeadline: "445"
            };
            
        }
        
        //alert(element.type.toString());
      });
       
return changeHistor;




};

export default Find;
}*/